import type { ResourceLanguage } from 'i18next';
import commons from './commons.json';
import discovery from './discovery.json';
import home from './home.json';
import notFound from './not-found.json';

const it: ResourceLanguage = {
  commons,
  discovery,
  home,
  notFound,
};

export default it;
